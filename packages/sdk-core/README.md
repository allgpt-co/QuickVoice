# QuickVoice SDK Core

Internal shared SDK contract helpers for generated TypeScript/Python clients and CLI behavior.

This package intentionally contains no generated resource clients yet. It defines the reviewed semantics that generated clients must share: base URL handling, redaction, retry/idempotency policy, pagination, typed API errors, and CLI exit codes.
